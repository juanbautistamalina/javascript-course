// undefined: Significa que la variable no tiene contenido.
let indefinida;

// null: Significa que la variable no tiene contenido, pero en este caso el programador asigna null intencionalmente
let nulo = null;

// NaN (Not a Number): Ocurre cuando se realizan operaciones con tipos de datos no numéricos
let noEsUnNumero = "hola" * 12;
console.log(noEsUnNumero);
